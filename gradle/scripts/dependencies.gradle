dependencies {
    // Registrate
    api(libs.registrate)

    // jts
    implementation(libs.jts)
    additionalRuntimeClasspath(libs.jts)
    jarJar(libs.jts)

    // Cloth Config
    implementation(libs.clothConfig)

    // AnvilCraft
    implementation(libs.anvilcraft) { transitive = false }

    // Curios API
    implementation(libs.curios)
}
